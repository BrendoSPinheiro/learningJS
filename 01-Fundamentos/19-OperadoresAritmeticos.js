const [ a, b, c, d ] = [20, 10, 5, 30];

// Operador binário

const soma = a + b + c + d;
const subtracao = a - b;
const multiplicacao = b * c;
const divisao = d / b;
const modulo = a % 2; // resto da divisão

console.log(`Soma = ${soma}, 
Subtração = ${subtracao}, 
Multiplicação = ${multiplicacao},
Divisão = ${divisao},
Módulo = ${modulo}
`);
